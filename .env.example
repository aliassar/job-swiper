# NextAuth.js Configuration
# Generate a random secret: openssl rand -base64 32
NEXTAUTH_SECRET=your-secret-here
NEXTAUTH_URL=http://localhost:3000

# GitHub OAuth
# Create an OAuth app at https://github.com/settings/developers
# Set callback URL to: http://localhost:3000/api/auth/callback/github
GITHUB_CLIENT_ID=your-github-client-id
GITHUB_CLIENT_SECRET=your-github-client-secret

# Google OAuth
# Create OAuth credentials at https://console.cloud.google.com/apis/credentials
# Add http://localhost:3000 to Authorized JavaScript origins
# Add http://localhost:3000/api/auth/callback/google to Authorized redirect URIs
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Backend API Configuration
# For local development, use: http://localhost:5000
# For production, use your backend server URL (e.g., https://api.jobswiper.app)
# 
# IMPORTANT: This connects to the job-swipper-server backend repository
# Repository: https://github.com/aliassar/job-swipper-server
# 
# Cross-repository dependency:
# - The backend server must be running for the frontend to function
# - Ensure the backend is configured and running before starting this app
# - Backend handles authentication, job data, and user actions
# - API endpoints are documented in the backend repository
NEXT_PUBLIC_API_URL=http://localhost:5000
